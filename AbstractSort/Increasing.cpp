#ifndef INCREASING_H
#define INCREASING_H
    #include "Increasing.h"
#endif // INCREASING_H

//---------------------------------------------------------------------------
/* compare() takes 2 integer values, checks if the first is greater than the
* second, and returns the result as a boolean.
*/
bool Increasing::compare(int a, int b)
{
    return a > b;
}
