#ifndef DECREASING_H
#define DECREASING_H
    #include "Decreasing.h"
#endif // DECREASING_H

//---------------------------------------------------------------------------
/* compare() takes 2 integer values, checks if the first is less than the
* second, and returns the result as a boolean.
*/
bool Decreasing::compare(int a, int b)
{
    return a < b;
}
